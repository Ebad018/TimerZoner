<Window x:Class="TimeZoner.AddCityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search City" Height="350" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox x:Name="SearchBox" Padding="5" Margin="0,0,0,10" 
                 TextChanged="SearchBox_TextChanged" 
                 ToolTip="Search for a city..." />
                 
        <ListBox x:Name="CityList" Grid.Row="1" Margin="0,0,0,10"
                 DisplayMemberPath="Name" 
                 MouseDoubleClick="CityList_MouseDoubleClick"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Width="70" Margin="0,0,10,0" Click="Add_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="70" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
