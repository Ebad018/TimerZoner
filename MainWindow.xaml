<Window x:Class="TimeZoner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeZoner"
        xmlns:vm="clr-namespace:TimeZoner.ViewModels"
        mc:Ignorable="d"
        Title="TimeZoner" Height="Auto" Width="Auto"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="False" SizeToContent="WidthAndHeight"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        ShowInTaskbar="False">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <!-- Context Menu for Right Click -->
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Settings" Click="MenuItem_Settings_Click"/>
            <Separator />
            <MenuItem Header="Exit" Click="MenuItem_Exit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border Background="#60000000" CornerRadius="15" Padding="20" Margin="10">
        <ItemsControl ItemsSource="{Binding Cities}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="90" />
                        </Grid.ColumnDefinitions>

                        <!-- City Name & Date -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Foreground="{Binding DataContext.ThemeColor, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding CurrentDate}" Foreground="{Binding DataContext.ThemeColor, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="12" Opacity="0.7" />
                        </StackPanel>

                        <!-- Time -->
                        <TextBlock Grid.Column="1" Text="{Binding CurrentTime}" Foreground="{Binding DataContext.ThemeColor, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="36" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0"/>

                        <!-- Weather -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding WeatherIcon}" FontSize="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TemperatureText}" Foreground="{Binding DataContext.ThemeColor, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="18" VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</Window>
